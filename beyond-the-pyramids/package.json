{
  "name": "beyond-the-pyramids",
  "private": true,
  "scripts": {
    "build": "npm run build:prism",
    "build:prism": "esbuild components/mount-prism.jsx --bundle --minify --outfile=js/prism-bundle.js --format=iife --jsx=automatic --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "dependencies": {
    "ogl": "^1.0.11",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
