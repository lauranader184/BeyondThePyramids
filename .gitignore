# Video files are too large for git hosting (GitHub rejects files over 100 MB).
# Keep the film locally, or host it on YouTube/Vimeo and embed it instead.
beyond-the-pyramids/video/
beyond-the-pyramids/node_modules/
